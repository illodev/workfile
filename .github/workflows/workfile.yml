# workfile:begin kind=ci-github version=0.1.0 digest=sha256:d4a70c82281be26c5a203dca40fae6a8af1d51e086834548d2f374ec11ab340c
# Generated by @illodev/workfile 0.1.0
name: Workfile

on:
  pull_request:
  push:
    branches: [main, master, dev]

permissions:
  contents: read

jobs:
  doctor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Validate repository protocol
        run: npx --yes @illodev/workfile@0.1.0 doctor --json
      - name: Check generated agent instructions
        run: npx --yes @illodev/workfile@0.1.0 agents check --json
# workfile:end
